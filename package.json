{
  "name": "sheets-weather-addon",
  "private": true,
  "type": "module",
  "scripts": {
    "build:server": "tsc -p tsconfig.server.json",
    "build:ui": "cd src/ui && npm i && npm run build",
    "bundle:ui": "node scripts/deploy-ui.mjs",
    "copy:manifest": "cp appsscript.json dist/appsscript.json",
    "build": "npm run build:server && npm run build:ui && npm run bundle:ui && npm run copy:manifest",
    "push": "clasp push",
    "deploy": "npm run build && npm run push"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.6.3"
  }
}